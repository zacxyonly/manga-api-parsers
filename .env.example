# ─────────────────────────────────────────────────────────────────────────────
# api-manga v2.0.0 — Environment Variables
#
# Salin file ini ke .env lalu sesuaikan:
#   cp .env.example .env
#
# Docker Compose otomatis membaca .env dari direktori yang sama dengan
# docker-compose.yml — tidak perlu --env-file secara eksplisit.
# ─────────────────────────────────────────────────────────────────────────────

# ── Server ────────────────────────────────────────────────────────────────────

# Port yang di-listen oleh server di DALAM container
PORT=8080

# Port yang di-expose ke host (mesin kamu / server)
# Ubah jika 8080 sudah dipakai, e.g. HOST_PORT=9090
HOST_PORT=8080

# ── CORS ──────────────────────────────────────────────────────────────────────

# Origins yang diizinkan, comma-separated.
# Kosongkan (atau hapus baris ini) untuk allow all origin (*) — aman untuk dev.
# Production: isi dengan domain frontend kamu, e.g.:
#   ALLOWED_ORIGINS=https://app.contoh.com,https://contoh.com
ALLOWED_ORIGINS=

# ── Cache TTL ─────────────────────────────────────────────────────────────────

# Semua nilai dalam DETIK

# Cache untuk list manga (popular / latest / search)
CACHE_TTL_LIST_SEC=300       # 5 menit

# Cache untuk detail manga + daftar chapter
CACHE_TTL_DETAIL_SEC=600     # 10 menit

# Cache untuk tags / genre — jarang berubah, aman di-cache lama
CACHE_TTL_TAGS_SEC=3600      # 1 jam

# Cache untuk URL halaman (page CDN links)
CACHE_TTL_PAGES_SEC=1800     # 30 menit
